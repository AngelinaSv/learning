version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: study_node_app
    volumes:
      - ./app:/usr/src/app
    ports:
      - "3009:3009"               
